<h1>Write you fucking review</h1>

<%= simple_form_for [@restaurant, @review] do |t| %>
    <div class="row justify-content-center">
      <div class="col-6">
        <%= t.input :content %>
        <%= t.input :rating, collection: (1..5) %>
        <%= t.submit%>
<% end %>
        <%= link_to 'Back', restaurant_path(@restaurant), class: "btn btn-outline-secondary" %>
      </div>
    </div>
